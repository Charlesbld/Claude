# Moteur de staffing / capacity planning — dépendances
pandas>=2.2
numpy>=1.26
pyarrow>=15        # parquet
pyyaml>=6.0        # config
holidays>=0.45     # calendriers par pays (A1)
streamlit>=1.30    # restitution (F)
plotly>=5.18       # heatmap / graphes (F)
pytest>=8.0        # validation des calculs
